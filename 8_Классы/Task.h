#pragma once

// Реализуйте класс дроби:
// class Fraction {
//   long long num_, den_;          // всегда den_ > 0
//  public:
//   Fraction(long long n = 0, long long d = 1);
//   double  value() const;
//   friend std::ostream& operator<<(std::ostream&, const Fraction&);
//  };

//  При создании сократить дробь на НОД и перенести знак в числитель.
//  value() — вернуть double.
//  Перегрузить оператор << вида a/b.

int main() {}