#pragma once

// Реализуйте класс дроби:
// class Fraction {
//   long long num_, den_;          // den_ всегда > 0
//  public:
//   Fraction(long long n = 0, long long d = 1);
//   double  value() const;
//   friend std::ostream& operator<<(std::ostream&, const Fraction&);
//  };

//  При создании, в конструкторе сократить дробь на НОД и перенести знак в
//  числитель.
//  Функция value() — должна вернуть саму дробь.
//  Перегрузите оператор << вида a/b:
//  operator/(...);

int main() {}